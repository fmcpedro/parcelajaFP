#app:
#    resource: '@AppBundle/Controller/'
#    type: annotation

homepage:
    path:      /{_locale}
    defaults:  { _controller: AppBundle:Default:index, _locale: en }
    requirements:
        _locale:  en|pt
        
        
email:
    path:      /{_locale}/email
    defaults:  { _controller: AppBundle:Default:email, _locale: en }
    requirements:
        _locale:  en|pt